import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Info = () => {
  return (
    <View>
      <Text>Info</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Info;
