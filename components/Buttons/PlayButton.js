import React from "react";
import IconAnt from "react-native-vector-icons/AntDesign";

const PlayButton = ({ size, color }) => {
  return <IconAnt name="play" size={size} color={color} />;
};

export default PlayButton;
